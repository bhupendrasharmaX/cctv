# Full runtime, including AI inference.
# torch arrives as a dependency of ultralytics/easyocr; install the CPU wheels
# explicitly if you do not have CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
-r requirements-core.txt
ultralytics>=8.1.0
easyocr>=1.7.0
